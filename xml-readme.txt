XSLT - extensible style language transformation, which transforms a xml with a xsl file into third document, says html.

SAX - Simple API for XML, make one pass through the document and react to events, 
like when a tag begins or ends

DOM - Document Object Model, the entire XML document is read into memory and essentially stored in a treelike data model